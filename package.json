{
  "name": "type-declaration",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "node index.js",
    "gen-types": "npx tsc src/index.js --declaration --allowJs --emitDeclarationOnly",
    "gen-types-json": "npx tsc -p tsconfig.build.types.json",
    "gen-types-specific-folder": "npx tsc src/index.js --declaration --allowJs --emitDeclarationOnly --outDir types",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.7.2"
  }
}
